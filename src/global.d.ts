declare module '*.svelte.civet' {
	// Re-export everything from the corresponding .ts file
	// Adjust the path if your .ts file is named differently or located elsewhere
	export * from './class.svelte.ts';
}
